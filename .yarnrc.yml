nodeLinker: node-modules

# Keep .yarn footprint small: no zipped Berry cache, no PnP runtime.
enableGlobalCache: true
enableTelemetry: false

# Yarn 4 defaults install to a hidden .yarn/cache; stay on the user-global
# cache instead so workspace clones do not duplicate the install.
